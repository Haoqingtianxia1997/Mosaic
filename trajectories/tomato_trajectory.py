# Trajectory for tomato
# Task: Grasp the tomato from the top and lift it gently upward, then place it at a different location
# Generated by TrajectoryGenerator

import numpy as np

import numpy as np

def generate_tomato_manipulation_trajectory(params):
    waypoints = []

    # Extract geometric parameters for the ellipsoid (tomato)
    ellipsoid = params['component_0']
    center = np.array(ellipsoid['center'])
    radii = np.array(ellipsoid['radii'])

    # Define approach and manipulation heights
    approach_z = center[2] + 0.10  # 10cm above the top of the tomato
    grasp_z = center[2] + radii[2]  # At the top surface of the tomato

    # Phase 1: START - Approach from above at a safe height
    start_pos = [center[0], center[1], approach_z]
    waypoints.append({
        'position': start_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Start: Safe approach position above tomato at height {:.3f}m'.format(approach_z)
    })

    # Phase 2: PRE-GRASP - Move closer to the grasp point
    pre_grasp_pos = [center[0], center[1], grasp_z + 0.05]  # 5cm above the grasp point
    waypoints.append({
        'position': pre_grasp_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Pre-grasp: Approaching top surface of tomato'
    })

    # Phase 3: GRASP - At surface contact (top of the ellipsoid)
    grasp_pos = [center[0], center[1], grasp_z]  # Exact contact with the top surface
    waypoints.append({
        'position': grasp_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Grasp: AT top surface of tomato'
    })

    # Phase 4: CLOSE - Close gripper on the tomato
    waypoints.append({
        'position': grasp_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Close gripper around tomato'
    })

    # Phase 5: LIFT - Lift the tomato upwards
    lift_pos = [center[0], center[1], grasp_z + 0.15]  # Lift 15cm upwards
    waypoints.append({
        'position': lift_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Lift tomato upwards'
    })

    # Phase 6: MOVE - Move to a new location while maintaining lifted height
    move_pos = [center[0] + 0.20, center[1] + 0.15, grasp_z + 0.15]  # Move 20cm in X and 15cm in Y
    waypoints.append({
        'position': move_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Move to new location while lifted'
    })

    # Phase 7: LOWER - Lower back to original grasp height
    lower_pos = [center[0] + 0.20, center[1] + 0.15, grasp_z]
    waypoints.append({
        'position': lower_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Lower tomato to original grasp height'
    })

    # Phase 8: END - At SAME HEIGHT as start, but DIFFERENT XY location
    end_pos = [center[0] + 0.20, center[1] + 0.15, approach_z]  # Same Z as start!
    waypoints.append({
        'position': end_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'End: Same height as start ({:.3f}m), moved to new XY location'.format(approach_z)
    })

    return waypoints
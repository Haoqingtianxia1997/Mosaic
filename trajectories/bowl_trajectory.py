# Trajectory for bowl
# Task: Grasp the bowl at the RIM EDGE from the side (where humans naturally hold bowls - at the opening edge, NOT at the dome bottom). The rim contact point is at center + horizontal_offset * radius, where center is at the opening plane. Lift 15cm upward and move to new location.
# Generated by TrajectoryGenerator

import numpy as np

import numpy as np

def generate_bowl_manipulation_trajectory(params):
    waypoints = []
    
    # Extract geometry of the bowl (hemisphere)
    bowl = params['component_0']
    center = np.array(bowl['center'])
    radius = bowl['radius']
    axis = np.array(bowl['axis'])
    
    # Define approach height (e.g., slightly above the rim)
    approach_z = center[2] + 0.05  # 5cm above the rim
    
    # Calculate horizontal offset direction (perpendicular to axis)
    horizontal_offset = np.array([1, 0, 0])  # Assuming approach from positive X direction
    
    # Phase 1: START - Approach from side at specific height
    start_pos = center + np.array([radius + 0.20, 0, 0])  # 20cm away in X direction
    start_pos[2] = approach_z  # Set Z to approach height
    waypoints.append({
        'position': start_pos.tolist(),
        'orientation': [0, 0, 0],  # Facing the bowl
        'gripper_state': 'open',
        'description': 'Start: Safe approach position from side at height {:.3f}m'.format(approach_z)
    })
    
    # Phase 2: PRE-GRASP - Move closer to rim
    pre_grasp_pos = center + horizontal_offset * (radius + 0.05)  # 5cm away from rim
    pre_grasp_pos[2] = approach_z  # Maintain approach height
    waypoints.append({
        'position': pre_grasp_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Pre-grasp: Approaching rim edge'
    })
    
    # Phase 3: GRASP - AT SURFACE (contact point on rim)
    grasp_pos = center + horizontal_offset * radius  # Exact contact with rim surface
    waypoints.append({
        'position': grasp_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Grasp: AT rim edge (radius={:.3f}m from center)'.format(radius)
    })
    
    # Phase 4: CLOSE - Close gripper around rim
    waypoints.append({
        'position': grasp_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Close gripper on rim edge'
    })
    
    # Phase 5: MANIPULATION - Lift bowl up 15cm
    lift_pos = grasp_pos + np.array([0, 0, 0.15])  # Lift 15cm upward
    waypoints.append({
        'position': lift_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Lift bowl upward'
    })
    
    # Phase 6: PLACE/LOWER - Move to new location while maintaining lifted height
    move_pos = lift_pos + np.array([0.20, 0.15, 0])  # Move 20cm in X and 15cm in Y
    waypoints.append({
        'position': move_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Move to new location while lifted'
    })
    
    # Phase 7: LOWER - Lower back to original approach height
    lower_pos = move_pos.copy()
    lower_pos[2] = approach_z  # Lower to original height
    waypoints.append({
        'position': lower_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Lower to original height'
    })
    
    # Phase 8: END - At SAME HEIGHT as start, but DIFFERENT XY location
    end_pos = lower_pos.copy()
    end_pos[0] += 0.15  # Ensure end position is different in XY (at least 15cm apart)
    waypoints.append({
        'position': end_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'End: Same height as start ({:.3f}m), but moved to new XY location'.format(approach_z)
    })
    
    return waypoints
# Trajectory for cookie_box
# Task: Grasp the box from the sides and slide it forward 30cm
# Generated by TrajectoryGenerator

import numpy as np

import numpy as np

def generate_cookie_box_trajectory(params):
    waypoints = []
    
    # Extract box geometry
    box = params['component_0']
    center = np.array(box['center'])
    dimensions = np.array(box['dimensions'])  # [width, depth, height]
    
    # Define approach height (center Z)
    approach_z = center[2]
    
    # Calculate contact points on the sides of the box
    # For side grasp, assume grasping at the middle of the sides
    side_contact_offset = np.array([dimensions[0]/2, 0, 0])  # Grasp at width/2 on the side
    contact_pos = center + side_contact_offset
    
    # Phase 1: START - Safe approach position
    start_pos = [center[0] - dimensions[0]/2 - 0.20, center[1], approach_z]
    waypoints.append({
        'position': start_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Start: Safe approach position from side at height {:.3f}m'.format(approach_z)
    })
    
    # Phase 2: PRE-GRASP - Move closer to grasp location
    pre_grasp_pos = [contact_pos[0] - 0.05, contact_pos[1], approach_z]
    waypoints.append({
        'position': pre_grasp_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Pre-grasp: Approaching side of box'
    })
    
    # Phase 3: GRASP - At surface contact
    waypoints.append({
        'position': contact_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'Grasp: At side surface of box'
    })
    
    # Phase 4: CLOSE - Close gripper around object
    waypoints.append({
        'position': contact_pos.tolist(),
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Close gripper on box'
    })
    
    # Phase 5: MANIPULATION - Slide the box forward 30cm
    slide_pos = [contact_pos[0] + 0.30, contact_pos[1], approach_z]
    waypoints.append({
        'position': slide_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Slide box forward by 30cm'
    })
    
    # Phase 6: PLACE/LOWER - Maintain the same height
    waypoints.append({
        'position': slide_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'close',
        'description': 'Maintain height after sliding'
    })
    
    # Phase 7: END - At SAME HEIGHT as start, but DIFFERENT XY location
    end_pos = [slide_pos[0] + 0.15, slide_pos[1], approach_z]  # Same Z as start, different XY
    waypoints.append({
        'position': end_pos,
        'orientation': [0, 0, 0],
        'gripper_state': 'open',
        'description': 'End: Same height as start ({:.3f}m), moved to new XY location'.format(approach_z)
    })
    
    return waypoints

# Example usage
params = {
    "component_0": {
        "type": "box",
        "center": [0.0, 0.0, 0.04023660452591979],
        "dimensions": [0.153, 0.10200000000000001, 0.08111732676712363]
    }
}

trajectory = generate_cookie_box_trajectory(params)
for waypoint in trajectory:
    print(waypoint)